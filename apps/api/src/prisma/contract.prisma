// use prisma-next

model User {
  id           String @id @default(uuid())
  username     String
  email        String @unique
  hashPassword String
  createdAt    DateTime @default(now())
  updatedAt    temporal.updatedAt()
}


